# Commented out for the new yml
 name: Say Hello
 on:
   push:
     branches: 
     - main

 jobs:
   hello-world:
     runs-on: ubuntu-latest
     steps:
     - name: Echo the message
       run: |
        echo "Hola Jesus David"
     - name: Dime la fecha
       run: Date
     - run: ls -l
